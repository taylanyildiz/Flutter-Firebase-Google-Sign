name: fire_base_google_sign_in
description: A new Flutter project.


publish_to: 'none'


version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  #State Management
  provider:
  #Firebase Auth
  firebase_core:
  firebase_auth:
  #Google Sign
  google_sign_in:

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:

  uses-material-design: true

  assets:
     - assets/images/google-symbol.png
